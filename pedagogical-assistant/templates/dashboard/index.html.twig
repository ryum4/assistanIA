<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Index</title>
    <style>
        body { margin: 0; padding: 0; font-family: sans-serif; }
        .container { 
            min-height: 100vh;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .content {
            text-align: center;
            color: white;
            z-index: 1;
        }
        h1 { font-size: 48px; margin-bottom: 20px; }
        p { font-size: 20px; margin-bottom: 40px; opacity: 0.9; }
        .btn-group {
            display: flex;
            gap: 20px;
            justify-content: center;
        }
        a {
            padding: 15px 40px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: transform 0.2s;
            display: inline-block;
        }
        .btn-primary {
            background: white;
            color: #667eea;
        }
        .btn-secondary {
            background: rgba(255,255,255,0.2);
            color: white;
            border: 2px solid white;
        }
        a:hover { transform: translateY(-3px); }
    </style>
</head>
<body>
    <div class="container">
        <div class="content">
            <h1>🎓 Pedagogical Assistant</h1>
            <p>Plateforme IA pour la génération de plans de cours et d'exercices pédagogiques</p>
            <div class="btn-group">
                <a href="/login" class="btn-primary">Se connecter</a>
                <a href="/api/doc" class="btn-secondary">API Documentation</a>
            </div>
        </div>
    </div>
</body>
</html>
